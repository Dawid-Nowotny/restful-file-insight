import { UploadFileData } from './upload-file-data.model';

describe('UploadFileData', () => {
    it('should create an instance', () => {
        expect(new UploadFileData()).toBeTruthy();
    });
});
