import { UploadImageData } from './upload-image-data.model';

describe('UploadImageData', () => {
    it('should create an instance', () => {
        expect(new UploadImageData()).toBeTruthy();
    });
});
